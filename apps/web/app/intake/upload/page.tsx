import { IntakeWrapper } from "@/libs";

// Cycle picker + drag-drop upload + scoring trigger, moved off the console
// landing route (design-system §6.3.1) — reached via the console's "Upload
// batch" action rather than being the console itself.
export default function IntakeUploadPage() {
  return <IntakeWrapper />;
}
