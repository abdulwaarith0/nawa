{
  "name": "nawa",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.13.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "ts.check": "turbo run ts.check",
    "test": "turbo run test",
    "contracts:generate": "node scripts/contracts-generate.mjs",
    "styleguide": "pnpm --filter web dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "turbo": "2.1.3",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
