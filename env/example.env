# GENERATED SOURCE — edit this file, never the per-app projections.
# scripts/sync-env projects this into services/api/.env and apps/web/.env.local.
# No real values belong in this file; env/development.env (gitignored) holds those.

# --- Core ---
ENVIRONMENT=development
DATABASE_URL=postgresql+asyncpg://nawa:nawa@localhost:5433/nawa_dev
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=

# --- Object storage (MinIO locally) ---
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=nawa-uploads

# --- AI providers ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
LLM_DEFAULT_PROVIDER=anthropic
EMBEDDINGS_PROVIDER=openai
EMBEDDINGS_DIMENSION=1536

# --- Web / routing ---
WEB_ORIGIN=http://localhost:3000
API_INTERNAL_URL=http://localhost:8000
